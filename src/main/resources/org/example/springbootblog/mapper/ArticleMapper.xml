<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.springbootblog.mapper.ArticleMapper">

<!--    分页查询文章列表-->
<!--    Page<Article> getArticleListPage(Page<Article> page, Integer categoryId, String state);-->
    <select id="getArticleListPage" resultType="org.example.springbootblog.pojo.Article">
        select * from article
        <where>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="state != null and state != ''">
                and state = #{state}
            </if>
        </where>
    </select>

<!--    根据id更新文章-->
<!--    void updateArticleById(Article article);-->
    <update id="updateArticleById" parameterType="org.example.springbootblog.pojo.Article">
        update article
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="coverImg != null and coverImg != ''">
                cover_img = #{coverImg},
            </if>
            <if test="state != null and state != ''">
                state = #{state},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>